info:
  name: internal_api_v1_image_destroy
  type: http
  seq: 3
  tags:
    - internal

http:
  method: DELETE
  url: "{{baseUrl}}/internal/api/v1/image/:id"
  headers:
    - name: X-Api-Key
      value: "{{apiKey}}"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: apikey
    key: X-Api-Key
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No response body
    request:
      url: "{{baseUrl}}/internal/api/v1/image/:id"
      method: DELETE
      headers:
        - name: X-Api-Key
          value: "{{apiKey}}"
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
