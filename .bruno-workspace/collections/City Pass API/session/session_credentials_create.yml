info:
  name: session_credentials_create
  type: http
  seq: 1
  tags:
    - session

http:
  method: POST
  url: "{{baseUrl}}/city-pass/api/v1/session/credentials"
  headers:
    - name: X-Session-Credentials-Key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
        "session_token": "",
        "encrypted_administration_no": ""
      }
  auth:
    type: apikey
    key: X-Session-Credentials-Key
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/city-pass/api/v1/session/credentials"
      method: POST
      headers:
        - name: X-Session-Credentials-Key
          value: "{{apiKey}}"
      body:
        type: json
        data: |-
          {
            "session_token": "",
            "encrypted_administration_no": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": ""
          }
  - name: 401 Response
    request:
      url: "{{baseUrl}}/city-pass/api/v1/session/credentials"
      method: POST
      headers:
        - name: X-Session-Credentials-Key
          value: "{{apiKey}}"
      body:
        type: json
        data: |-
          {
            "session_token": "",
            "encrypted_administration_no": ""
          }
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "code": "TOKEN_INVALID",
            "detail": ""
          }
