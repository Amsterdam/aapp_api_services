info:
  name: parking_api_v1_egis_proxy_retrieve
  type: http
  seq: 8
  tags:
    - parking

http:
  method: GET
  url: "{{baseUrl}}/parking/api/v1/egis-proxy/:path"
  headers:
    - name: X-Api-Key
      value: "{{apiKey}}"
  params:
    - name: path
      value: ""
      type: path
  auth:
    type: apikey
    key: X-Api-Key
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: No response body
    request:
      url: "{{baseUrl}}/parking/api/v1/egis-proxy/:path"
      method: GET
      headers:
        - name: X-Api-Key
          value: "{{apiKey}}"
      params:
        - name: path
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
