info:
  name: mijnamsterdam_api_v1_device_destroy
  type: http
  seq: 2
  tags:
    - mijnamsterdam

http:
  method: DELETE
  url: "{{baseUrl}}/mijnamsterdam/api/v1/device"
  headers:
    - name: DeviceId
      value: ""
    - name: X-Api-Key
      value: "{{apiKey}}"
  auth:
    type: apikey
    key: X-Api-Key
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/mijnamsterdam/api/v1/device"
      method: DELETE
      headers:
        - name: DeviceId
          value: ""
        - name: X-Api-Key
          value: "{{apiKey}}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "status": "OK",
            "profile_name": ""
          }
  - name: 401 Response
    request:
      url: "{{baseUrl}}/mijnamsterdam/api/v1/device"
      method: DELETE
      headers:
        - name: DeviceId
          value: ""
        - name: X-Api-Key
          value: "{{apiKey}}"
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "code": "API_KEY_INVALID",
            "detail": ""
          }

docs: |-
  Mixin to pre-process the request to get the device id
  and bind it to the view.
  This mixin is used specifically for DRF views.
