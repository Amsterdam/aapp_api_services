info:
  name: boat_charging_api_v1_session_cancel
  type: http
  seq: 10
  tags:
    - boat-charging

http:
  method: POST
  url: "{{baseUrl}}/boat-charging/api/v1/sessions/:session_id/cancel"
  headers:
    - name: Access-Token
      value: "{{access-token}}"
    - name: X-Api-Key
      value: "{{apiKey}}"
    - name: DeviceId
      value: bruno-user
  params:
    - name: session_id
      value: 084ebc90-e1a2-463b-b21f-2d912204e619
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/boat-charging/api/v1/session/:charging_station_id"
      method: POST
      headers:
        - name: Access-Token
          value: "{{access-token}}"
        - name: X-Api-Key
          value: "{{apiKey}}"
      params:
        - name: charging_station_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "evse_id": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "transaction_ids": []
          }

docs: This generic API view supports async pagination.
