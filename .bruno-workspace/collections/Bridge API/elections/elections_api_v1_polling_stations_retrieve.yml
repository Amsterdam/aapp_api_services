info:
  name: elections_api_v1_polling_stations_retrieve
  type: http
  seq: 1
  tags:
    - elections

http:
  method: GET
  url: "{{baseUrl}}/elections/api/v1/polling-stations"
  headers:
    - name: X-Api-Key
      value: "{{apiKey}}"
  auth:
    type: apikey
    key: X-Api-Key
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: No response body
    request:
      url: "{{baseUrl}}/elections/api/v1/polling-stations"
      method: GET
      headers:
        - name: X-Api-Key
          value: "{{apiKey}}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
