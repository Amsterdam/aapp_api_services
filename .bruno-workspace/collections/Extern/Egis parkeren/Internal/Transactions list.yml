info:
  name: Transactions list
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/v1/ssp/wallet_transaction/list?page=1&row_per_page=10&sort=paid_at:desc&filters[status]=COMPLETED"
  params:
    - name: page
      value: "1"
      type: query
    - name: row_per_page
      value: "10"
      type: query
    - name: sort
      value: paid_at:desc
      type: query
    - name: filters[status]
      value: COMPLETED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
