info:
  name: Sessions list
  type: http
  seq: 7

http:
  method: GET
  url: "{{egis_base_url}}/api/v1/ssp/parking_session/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
