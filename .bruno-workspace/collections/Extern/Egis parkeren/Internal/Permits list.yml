info:
  name: Permits list
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/permit_overview/product_list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
