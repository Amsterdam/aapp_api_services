info:
  name: contact_api_v1_health_check_retrieve
  type: http
  seq: 2
  tags:
    - contact

http:
  method: GET
  url: "{{baseUrl}}/contact/api/v1/health-check"
  headers:
    - name: X-Api-Key
      value: "{{apiKey}}"
  auth:
    type: apikey
    key: X-Api-Key
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: No response body
    request:
      url: "{{baseUrl}}/contact/api/v1/health-check"
      method: GET
      headers:
        - name: X-Api-Key
          value: "{{apiKey}}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
